# Account Matrix PC 端产品化与订阅制改造建议

日期：2026-08-01  
评审视角：把当前 PC 端应用作为面向付费用户交付的商业产品，而不是内部工具或源码项目。

## 1. 总体判断

当前应用已经具备“运营工作台”的核心雏形：账号、浏览器环境、任务执行、目标互动、调度、评论素材、执行记录、统计报表、Gmail 初始化、诊断和系统配置都已串起来。

但如果直接把现在的界面给普通用户使用，风险很高。主要问题不是功能不够，而是产品边界还没有收敛：页面里同时暴露了业务功能、工程实现、脚本入口、本机路径、SQLite、`accounts.yaml`、PID、stdout/stderr、`run.lock`、平台预留能力、迁移工具和危险操作。普通用户会觉得复杂，客服成本会高；付费订阅用户还会天然要求账号体系、授权校验、套餐权益、到期处理、发票/订单、售后和数据安全说明。

建议把当前应用拆成三层：

1. **用户可见层**：日常运营需要的功能，语言要业务化、结果化。
2. **管理员**：账号批量导入、浏览器环境、调度策略、通知、素材库等需要谨慎配置的功能。
3. **开发层**：诊断、原始日志、路径、迁移、脚本输出、运行锁、源码模式等，默认不出现在普通用户导航里。

## 2. 当前页面是否适合给用户看

| 页面/区域             | 当前价值                                                   | 是否适合直接给用户看                       | 建议处理                                                                                                                                                                                                          |
| --------------------- | ---------------------------------------------------------- | ------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 首页                  | 总览账号、BitBrowser API、今日任务、平台账号概览           | **适合，但需要改文案和隐藏技术细节**       | 保留为默认首页。把“BitBrowser API 不可用”改成“浏览器服务未连接”，把“运行模式、账号配置、最近读取”等技术状态收进“系统健康”详情。                                                                                   |
| 平台选择器 / 更多平台 | 多平台能力矩阵，TikTok 已支持，Instagram/WhatsApp/抖音预留 | **不适合对普通用户突出展示**               | 普通版只展示已购买且可用的平台。预留平台不要出现在主界面，避免用户误以为已可用。平台能力矩阵放到“关于/版本能力”或管理员页。                                                                                       |
| 账号管理              | 账号增删改、启停、浏览器绑定、登录凭据、TikTok 注册        | **适合，但要分普通视图和高级视图**         | 普通用户看账号状态、启用、登录状态、最近结果、注册/修复入口。`profile_id`、代理、IP 分组、班次、凭据引用、清理数据放高级模式。删除账号、批量注册必须二次确认并记录审计。                                          |
| 浏览器环境            | BitBrowser/Chromium、代理检测、批量创建、账号同步          | **不适合直接给所有用户看**                 | 对普通用户改成“浏览器连接/环境检查”向导。批量创建 profile、代理导入、账号同步、清理 user data dir 属于管理员功能。不要展示完整代理、路径、CDP、profile 原始 ID。                                                  |
| 养号任务              | FYP 浏览、点赞、关注、评论参数，启动任务                   | **适合，但需要产品化包装和限制**           | 保留为核心付费功能。把概率、阈值、session 等术语转成“低/中/高强度”预设，仍允许高级用户精调。启动前展示风险提示、账号数、预计时长、今日额度。                                                                      |
| 目标号互动            | 目标号、参与账号、点赞/评论/关注概率、水位线和统计         | **适合高级用户，不适合默认开放给所有用户** | 作为 Pro/Team 功能。必须加用量限制、频率限制、异常熔断、评论素材质量检查和合规提示。水位线可改名为“已处理进度”，隐藏 `video_id`。                                                                                 |
| 调度计划              | scheduler 服务、每日触发次数、Jobs、run.lock、IP 冲突      | **适合管理员，不适合普通用户直接看**       | 普通版改成“自动运行计划”：每天次数、运行时间段、下次运行、冲突提示。隐藏 PID、`run.lock`、health URL、Job ID、`fires_per_day` 字段名。                                                                            |
| 评论素材              | 通用评论池、品牌评论池、重复检查、批量粘贴                 | **适合用户看**                             | 保留。增加敏感词/重复度/过短内容提醒，按平台和任务场景分类。保存前显示有效条数。                                                                                                                                  |
| 执行记录              | 动作记录、目标互动、目标关注明细                           | **适合，但需要翻译技术字段**               | 保留为“任务记录”。把 `fyp_browse`、`target_engagement`、`ok/error/skip`、`liked/commented` 翻译成中文业务状态。详情默认折叠，技术详情只给开发者排障使用。                                                         |
| Session 日志          | 原始 `sessions.log`、结构化日志、清空日志                  | **不适合普通用户主导航显示**               | 默认隐藏。普通用户只看任务记录和失败原因。管理员/运营可收集脱敏支持包，开发者需要时查看技术日志。                                                                                                                 |
| 统计报表              | 账号、OK/ERR/SKIP、视频、点赞、关注、评论、CSV 导出        | **适合用户看**                             | 保留为核心价值页。把 OK/ERR/SKIP 改为成功/失败/跳过。增加趋势图、失败率、账号健康分、套餐用量。                                                                                                                   |
| Gmail 初始化          | 单账号/批量邮箱初始化、密码、文件路径、步骤日志            | **只适合特定 onboarding/高级用户**         | 不应默认给所有用户。放到“账号初始化”流程里，弱化脚本概念。密码和邮箱文件需要更强提示、加密、权限说明和操作审计。                                                                                                  |
| 诊断工具              | 点赞/评论诊断、选择器、HTML、stdout/stderr                 | **不适合普通用户看**                       | 仅开发者排障使用。普通用户遇到问题应点击“生成支持包/联系管理员/运营”。                                                                                                                                            |
| 系统设置              | 运行模式、Python、路径、通知、迁移、危险操作               | **部分适合管理员，绝大部分不适合普通用户** | 普通用户只看订阅、通知、浏览器服务连接、自动关闭、版本信息。当前本地固定授权弹窗已移除；对外版本仍应把 Python、源码模式、路径、迁移、SQLite、清理日志隐藏到管理员/开发者权限下，并由 License 和后端命令校验控制。 |
| 任务运行输出面板      | PID、队列、stdout/stderr、暂停/强停、人工接管              | **适合保留，但需简化**                     | 普通视图显示任务状态、当前账号、进度、失败原因、继续/跳过/停止。命令、stdout/stderr、PID 放“技术详情”。                                                                                                           |
| 浏览器预览            | 观察任务浏览器画面                                         | **适合保留**                               | 作为任务详情的一部分，但要提示“仅用于观察，人工验证需在真实浏览器窗口完成”。                                                                                                                                      |

## 3. 建议对外导航

建议把左侧菜单调整为更像产品，而不是工程控制台：

1. **首页**
2. **账号**
3. **浏览器环境**
4. **任务中心**
5. **自动运行**
6. **评论素材**
7. **任务记录**
8. **数据报表**
9. **订阅与设置**
10. **帮助与支持**

其中：

- “目标号互动”可以放在“任务中心”内作为高级任务。
- “Gmail 初始化”可以放在“账号 > 初始化账号”里。
- “Session 日志”“诊断工具”“平台设置”“多平台迁移”“高级路径配置”不进入普通用户主导航。
- Instagram、WhatsApp、抖音如果只是预留，不应在用户购买页或主导航里展示为可用功能；最多在“即将支持”中说明。

## 4. 不适合给用户看的内容清单

这些内容建议默认隐藏，只在开发者排障、管理员/运营收集的脱敏支持包中出现；不要使用本地硬编码授权码作为保护边界：

- `accounts.yaml`、`comments.txt`、`comments_brand.txt`、`actions.db`、`sessions.log`、`run.lock`
- 本机绝对路径、用户目录、源码目录、运行时清单路径
- Python 可执行文件、源码开发模式、脚本名、命令行参数
- PID、health URL、Job ID、CDP endpoint、stdout、stderr
- `profile_id`、`bitbrowser_profile_id`、完整代理、凭据引用
- SQLite 状态、迁移预览、迁移应用、配置备份路径
- `fyp_browse`、`target_engagement`、`target_follow` 等内部动作码
- `true/false`、`ok/error/skip/failed/stopped` 等未产品化状态值
- 清理 `run.lock`、清空日志、清理 Chromium 数据等危险操作
- 未接入真实执行的平台入口和“预留任务”按钮

## 5. 订阅制需要新增什么

订阅制不是只加支付按钮。至少需要补齐以下模块。

### 5.1 账号与授权

必须新增：

- 用户注册、登录、找回密码
- 组织/团队空间，至少支持个人版和团队版
- 设备绑定：限制同一订阅可激活几台电脑
- License Token 或登录态：桌面端启动时校验订阅状态，不使用本地固定授权码
- 离线授权缓存：允许短期离线使用，例如 24-72 小时
- 到期宽限期：避免网络波动导致任务中断
- 强制下线/解绑设备能力
- 用户角色：普通用户、高级用户、管理员/运营、开发者

桌面端不能只靠前端隐藏按钮。所有关键 Tauri 后端命令也要校验授权，例如：

- 启动任务
- 启动调度
- 新增/启用账号
- 批量创建浏览器环境
- 导出统计
- 目标号互动
- Gmail 初始化

### 5.2 套餐权益

建议按“时间 + 账号规模 + 功能模块”组合收费。

| 套餐                 | 适合用户   | 建议限制                                                                  |
| -------------------- | ---------- | ------------------------------------------------------------------------- |
| Starter（月付/年付） | 小规模试用 | 账号上限 5-10 个；手动任务；基础记录和统计；不含目标号互动或只给低额度。  |
| Pro（月付/年付）     | 单人运营   | 账号上限 30-50 个；自动调度；评论素材；CSV 导出；目标号互动有限额度。     |
| Team（月付/年付）    | 小团队     | 账号上限 100-300 个；多席位；高级调度；目标号互动；支持包；更多日志保留。 |
| Enterprise           | 大客户     | 自定义账号量、设备数、平台能力、私有部署、专属支持、合同和发票。          |

建议计费维度：

- 可启用账号数量
- 可绑定设备数量
- 每日可执行账号次数
- 调度是否可用
- 目标号互动是否可用
- 目标号数量
- 每日目标互动次数
- 评论素材库数量
- 数据报表保留天数
- CSV/支持包导出
- 团队席位
- 问题处理方式和支持包保留策略

### 5.3 订阅中心页面

需要新增“订阅与设置”页面，至少展示：

- 当前套餐
- 到期时间
- 月付/年付状态
- 已用账号数 / 套餐上限
- 今日任务用量 / 今日额度
- 已绑定设备
- 升级、续费、取消订阅入口
- 发票/订单记录入口
- 到期、欠费、宽限期、试用期提示

任务启动前要展示和校验：

- 当前套餐是否可启动此任务
- 本次将消耗多少额度
- 是否超过启用账号上限
- 是否超过并发限制
- 是否超过目标互动限制

### 5.4 服务端与支付

当前项目看起来主要是本机桌面应用和本地配置，没有订阅后端模型。商业化至少要新增一个授权/计费服务。

服务端需要保存：

- 用户表
- 组织表
- 订阅表
- 套餐表
- 订单表
- 支付流水表
- 设备激活表
- License Token 表
- 用量记录表
- 审计日志表

支付渠道建议按目标市场选择：

- 面向海外用户：Stripe、Paddle 等订阅支付方案。
- 面向中国大陆用户：微信支付、支付宝，或接入现成 SaaS 订阅/发卡系统。
- 面向企业客户：线下合同、人工开通 License、发票和到期提醒。

支付 webhook 必须驱动订阅状态变化，例如：

- 首次支付成功
- 续费成功
- 支付失败
- 订阅取消
- 退款
- 到期
- 试用结束

### 5.5 License 设计

桌面端建议使用“在线校验 + 本地签名缓存”的方式。

基本流程：

1. 用户登录。
2. 桌面端提交设备指纹和应用版本。
3. 服务端返回签名 License。
4. 本地保存 License 缓存。
5. 每次启动和启动任务前校验 License。
6. 联网时同步用量和订阅状态。
7. 离线超过宽限期后禁止启动新任务，但允许查看本地历史数据。

License 内容建议包含：

- 用户 ID、组织 ID
- 套餐 ID
- 订阅状态
- 过期时间
- 设备 ID
- 可启用账号上限
- 功能开关
- 今日/周期额度
- 签名和签发时间

### 5.6 用量与风控

订阅制必须有用量统计，否则套餐无法落地。

建议记录：

- 每天启动任务次数
- 每天实际执行账号数
- 每天目标号互动次数
- 每天注册账号次数
- 当前启用账号数
- 当前绑定设备数
- 失败率、异常停止次数
- 连续高频操作告警

风控建议：

- 超过失败率阈值自动暂停调度
- 单账号连续失败自动停用调度
- 目标号互动超过安全频率自动降速
- 评论重复度过高时禁止保存或强提醒
- 到期/欠费时只允许查看，不允许启动新任务

### 5.7 法务与合规

当前功能涉及第三方平台账号、浏览器环境、自动化浏览、点赞、关注、评论和注册流程。商业化前必须补：

- 用户协议
- 隐私政策
- 订阅与退款条款
- 可接受使用政策
- 第三方平台账号风险提示
- 数据本地存储说明
- 密码和凭据存储说明
- 免责声明：不承诺账号安全、流量结果或平台不会限制账号

产品文案上建议避免把能力包装成“刷量、涨粉保证、规避风控”。更稳妥的表达是“多账号运营辅助、自动化任务编排、账号状态管理、素材管理、执行记录和统计”。

### 5.8 用户问题处理流程

第一版不设置独立排障角色。问题处理按小团队流程走：

```text
用户（普通/高级）
  ↓ 遇到问题
管理员/运营
  ↓ 收集问题描述、截图、版本、设备状态、脱敏支持包
开发者
  ↓ 定位、修复、打包、发布新版本
用户更新后继续使用
```

对应产品能力：

- PC 端保留“帮助与支持”页面，用于生成脱敏支持包、复制错误摘要、查看版本和订阅状态。
- 管理员/运营后台可查看用户订阅、设备、License、应用版本、最近用量和支持包摘要。
- 开发者可查看开发排障所需的详细脱敏支持包、技术日志、runtime 诊断和错误摘要。
- 管理员/运营不需要看到原始脚本输出、完整本机路径、完整代理或危险操作入口。
- 开发者不建议拥有修改套餐、退款、封禁等运营写权限。

## 6. 产品化优先级

### P0：对外发布前必须完成

- 隐藏 Session 日志、诊断工具、平台设置、迁移工具、路径配置、源码模式。
- 首页、任务、记录、统计的技术字段改成业务字段。
- 新增订阅中心占位页，即使先手动开通 License，也要让用户知道套餐、到期时间和账号额度。
- 后端命令增加 License 校验，不能只在前端禁用按钮。
- 增加套餐限制：启用账号数、任务启动、调度、目标号互动。
- 增加到期/离线/超额状态处理。
- 增加用户协议、隐私政策、风险提示。
- 支持生成脱敏支持包，而不是让用户截图 stdout/stderr。

### P1：第一版商业化

- 完整账号登录和设备绑定。
- 支付 webhook 和订阅状态自动同步。
- 套餐升级/续费/取消流程。
- 用量看板。
- 任务异常熔断。
- 评论素材质量检查。
- 自动更新和版本兼容检查。
- 管理员后台查看用户订阅、设备、License、支持包摘要和基础用量。

### P2：规模化运营

- 团队席位和角色权限。
- 多平台真实接入后按平台售卖。
- 云端配置备份和迁移。
- 更完整的数据报表和趋势图。
- 企业私有部署 License。
- 更细粒度的功能开关和灰度发布。

## 7. 建议的第一版商业化形态

如果目标是尽快把当前 PC 端变成可卖产品，建议第一版不要开放所有能力。

第一版推荐开放：

- 首页
- 账号
- 任务中心：TikTok FYP 养号
- 自动运行：简化版调度
- 评论素材
- 任务记录
- 数据报表
- 订阅与设置：套餐、到期、通知、浏览器连接
- 帮助与支持：导出支持包

第一版不推荐默认开放：

- 目标号互动：放 Pro/Team，高级开关
- Gmail 初始化：放账号初始化向导，不单独做主菜单
- 浏览器环境批量创建和账号同步：只给管理员
- Session 日志：隐藏
- 诊断工具：隐藏
- 平台设置和预留平台：隐藏
- 系统高级路径、迁移、源码模式、危险操作：默认隐藏；当前 PC 端已移除本地授权弹窗，商业化版本需要用角色/License 接管访问控制

## 8. 结论

这个应用可以产品化，但当前更像“内部运营/技术工具”。真正给付费用户使用时，核心不是继续堆功能，而是把用户视角收窄：

- 普通用户只看到账号、任务、计划、素材、记录、报表和订阅。
- 管理员才看到浏览器环境、批量导入、调度细节和通知。
- 开发者才看到日志、路径、脚本输出、诊断、迁移和危险操作；管理员/运营负责收集用户反馈和脱敏支持包，再转交开发者处理。

订阅制需要从产品、后端、桌面端授权、支付、用量、法务和客服一起补齐。否则用户付费后遇到最多的问题会是：不知道自己买了什么、为什么不能运行、到期后发生什么、账号额度怎么算、出了问题谁来定位。
