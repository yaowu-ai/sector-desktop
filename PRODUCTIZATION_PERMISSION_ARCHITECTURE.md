# Account Matrix 权限体系与管理端技术方案

日期：2026-08-1  
适用范围：Account Matrix PC 桌面端产品化、订阅制、用户权限、管理员后台、开发者排障和用户问题反馈流程设计。

## 1. 结论

建议第一版拆成 **PC 桌面端 + 管理/授权服务端** 两个实际交付端。

只靠当前这一个 PC 应用不够。当前 PC 应用适合承载订阅用户侧功能，也可以区分普通用户、高级用户和开发者排障模式；但不适合用来管理所有订阅用户、订单、设备、套餐、License 和用量。

同时，“订阅授权服务端”和“Web 管理后台”**不必须做成两个独立项目**。更务实的做法是新增一个 `account-matrix-server`，它同时包含：

- 面向 PC 桌面端的授权/API 能力。
- 面向管理员、运营和开发者的 Web 管理后台。
- 支付 webhook、订阅状态同步、设备绑定、License 签发和用量统计。

推荐形态：

| 端                                      | 使用者                                         | 作用                                                                                                                                         |
| --------------------------------------- | ---------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| PC 桌面端                               | 订阅用户、普通操作员、高级用户、开发者排障人员 | 执行账号管理、任务、调度、素材、记录、报表等本机功能。                                                                                       |
| 管理/授权服务端 `account-matrix-server` | PC 端、应用管理员、运营、开发者                | 对 PC 端提供登录、订阅校验、License、用量上报等 API；同时提供 Web 管理后台管理用户、组织、订阅、订单、套餐、设备、用量、封禁、支持包和审计。 |

第一版不建议拆成 `auth-server`、`admin-web`、`billing-server` 多个项目。除非后续规模、团队或合规要求变高，否则一个服务端项目内按模块隔离即可。

## 2. 人群与角色

建议角色不要直接绑定页面，而是绑定权限和功能开关。

| 人群        | 建议角色        | 使用端                                        | 主要权限                                                                             |
| ----------- | --------------- | --------------------------------------------- | ------------------------------------------------------------------------------------ |
| 普通用户    | `normal`        | PC 桌面端                                     | 首页、账号基础管理、启动允许的任务、查看记录/报表、查看订阅状态。                    |
| 高级用户    | `advanced`      | PC 桌面端                                     | 浏览器环境、批量导入、调度高级参数、目标号互动、高级任务参数、通知配置。             |
| 开发者      | `developer`     | 开发环境 + 内部排障模式 + 管理/授权服务端后台 | 查看脱敏支持包、原始技术日志、runtime 诊断、迁移、脚本输出、危险操作和开发调试信息。 |
| 管理员/运营 | `admin` / `ops` | 管理/授权服务端后台                           | 管理全部用户、订阅、套餐、订单、设备、License、封禁、用量和用户问题处理。            |

## 3. 为什么管理员不建议放在 PC 应用里

管理员需要管理的是中心化数据，而 PC 应用运行在用户自己的机器上，不能作为可信后台。

主要原因：

- PC 应用可被用户安装、复制、逆向或篡改，不适合放全局管理入口。
- 管理员需要跨用户查看订阅、订单、设备、License 和用量，这些数据必须在云端。
- 支付 webhook、续费、退款、到期、封禁、解绑设备都需要服务端处理。
- 运营人员不应该依赖用户侧自动化工具来管理客户。
- 如果把管理员功能塞进 PC 端，权限泄漏和误操作风险更高。

因此：

- 普通/高级用户权限可以放在 PC 应用中。
- 开发者排障模式可以放在 PC 应用中，但必须由服务端签发短期授权。
- 管理所有订阅用户的功能必须放在云端后台中。
- 云端后台可以和订阅授权 API 合并在同一个 `account-matrix-server` 项目里。

## 4. PC 桌面端权限设计

### 4.1 普通用户可见页面

普通用户建议只看到稳定、业务化、低风险页面：

- 首页
- 账号
- 任务中心
- 自动运行
- 评论素材
- 任务记录
- 数据报表
- 订阅与设置
- 帮助与支持

### 4.2 高级用户额外可见页面

高级用户可以看到更多配置能力：

- 浏览器环境
- 批量创建 profile
- 账号同步
- 目标号互动
- 调度高级参数
- 通知配置
- 支持包导出

### 4.3 开发者排障模式可见页面

这些页面不应出现在普通导航中：

- Session 日志
- 诊断工具
- stdout/stderr 技术详情
- 本机路径
- runtime 诊断
- 配置迁移
- 清理日志
- 清理 `run.lock`
- 清理 Chromium 数据
- 源码开发模式

## 5. 前端路由权限

当前项目是 Tauri + React，可以在路由配置中加入权限字段。

示例：

```ts
type UserRole = "normal" | "advanced" | "admin" | "developer";

interface ProtectedRoute {
  key: string;
  label: string;
  roles: UserRole[];
  features?: string[];
  element: JSX.Element;
}

const routes: ProtectedRoute[] = [
  { key: "home", label: "首页", roles: ["normal", "advanced"] },
  { key: "accounts", label: "账号", roles: ["normal", "advanced"] },
  { key: "tasks", label: "任务中心", roles: ["normal", "advanced"] },
  { key: "browser", label: "浏览器环境", roles: ["advanced"] },
  {
    key: "target-engagement",
    label: "目标号互动",
    roles: ["advanced"],
    features: ["target_engagement"],
  },
  { key: "sessions", label: "技术日志", roles: ["developer"] },
  { key: "diagnostic", label: "诊断工具", roles: ["developer"] },
];
```

登录后从服务端获取 License Claims：

```ts
interface LicenseClaims {
  userId: string;
  orgId: string;
  plan: "starter" | "pro" | "team" | "enterprise";
  roles: UserRole[];
  features: string[];
  limits: {
    maxEnabledAccounts: number;
    maxDevices: number;
    dailyTaskRuns: number;
    targetEngagement: boolean;
    scheduler: boolean;
    exportCsv: boolean;
  };
  expiresAt: string;
  offlineUntil: string;
}
```

前端根据 `roles`、`features`、`limits` 控制：

- 菜单是否显示
- 页面是否可进入
- 按钮是否可点击
- 表单字段是否可编辑
- 高级区域是否展示或可编辑
- 超额提示和升级入口

但前端隐藏只能改善体验，不能作为安全边界。

## 6. Tauri 后端命令权限

必须在 Tauri 后端命令层再次校验权限。否则用户可以绕过前端，直接调用命令。

需要校验的命令包括：

| 命令/能力                         | 校验点                                                                    |
| --------------------------------- | ------------------------------------------------------------------------- |
| `run_platform_task`               | 订阅是否有效、套餐是否允许任务、今日额度是否足够、账号数是否超限。        |
| `start_scheduler`                 | 套餐是否支持自动调度、License 是否有效、设备是否绑定。                    |
| `save_accounts`                   | 启用账号数是否超过套餐上限。                                              |
| `save_target_engagement_settings` | 是否购买目标号互动功能。                                                  |
| `run_gmail_setup`                 | 是否开放账号初始化/注册辅助功能。                                         |
| `run_python_script`               | 普通用户禁止调用诊断脚本。                                                |
| `clear_run_lock`                  | 仅开发者可用。                                                            |
| `clear_session_log`               | 仅开发者可用。                                                            |
| `apply_config_migration`          | 仅开发者可用。                                                            |
| `export_support_bundle`           | 普通用户可生成脱敏支持包，管理员/运营可查看摘要，开发者可查看详细脱敏包。 |

建议校验链路：

```text
React 前端路由和按钮权限
  ↓
Tauri command 权限校验
  ↓
runtime/Python 启动前校验必要授权
  ↓
服务端记录用量和审计日志
```

## 7. License 机制

PC 桌面端建议采用“在线校验 + 本地签名缓存”。

基本流程：

1. 用户在 PC 端登录。
2. PC 端生成或读取设备 ID。
3. PC 端向服务端提交用户身份、设备 ID、应用版本。
4. 服务端检查订阅、设备数量、封禁状态。
5. 服务端返回签名 License。
6. PC 端本地缓存 License。
7. 启动应用和启动关键任务前校验 License。
8. 联网时同步用量和最新订阅状态。
9. 离线超过宽限期后禁止启动新任务，但允许查看本地历史数据。

License 建议字段：

```json
{
  "userId": "user_123",
  "orgId": "org_123",
  "plan": "pro",
  "roles": ["advanced"],
  "features": ["scheduler", "target_engagement", "export_csv"],
  "limits": {
    "maxEnabledAccounts": 50,
    "maxDevices": 2,
    "dailyTaskRuns": 200,
    "dailyTargetInteractions": 1000
  },
  "expiresAt": "2026-08-31T23:59:59Z",
  "offlineUntil": "2026-08-03T23:59:59Z",
  "deviceId": "device_xxx",
  "issuedAt": "2026-07-31T08:00:00Z",
  "signature": "..."
}
```

## 8. 管理/授权服务端

服务端是订阅制的核心，不建议省略。但第一版可以把授权 API、Web 管理后台、支付 webhook 和用量统计放在同一个项目中，例如：

```text
account-matrix-server
  - API：给 PC 桌面端登录、校验 License、上报用量
  - Admin Web：给管理员管理用户、订阅、设备、订单
  - Billing：处理支付 webhook
  - Database：保存用户、套餐、订阅、设备、用量
```

PC 桌面端只连接这个 `account-matrix-server`。管理员也通过这个服务端提供的 Web 后台登录管理。

### 8.1 核心模块

- 用户注册/登录
- 组织/团队
- 角色与权限
- 套餐
- 订阅
- 订单
- 支付 webhook
- 设备绑定
- License 签发
- 用量统计
- 审计日志
- 支持包上传/查看
- 封禁和风控
- Web 管理后台页面

### 8.2 建议数据表

第一版可以包含：

- `users`
- `organizations`
- `organization_members`
- `plans`
- `subscriptions`
- `orders`
- `payment_events`
- `devices`
- `licenses`
- `usage_daily`
- `feature_flags`
- `support_bundles`
- `audit_logs`

### 8.3 面向 PC 端的关键 API

建议至少提供：

- `POST /auth/login`
- `POST /auth/refresh`
- `POST /devices/activate`
- `POST /devices/deactivate`
- `GET /license/current`
- `POST /license/refresh`
- `POST /usage/report`
- `GET /subscription/current`
- `GET /plans`
- `POST /billing/checkout`
- `POST /billing/webhook`
- `POST /support-bundles`

### 8.4 Web 管理后台

管理后台给管理员/运营和开发者使用。第一版不设置独立排障角色：用户遇到问题先联系管理员/运营，管理员/运营收集问题描述、截图、应用版本、设备状态和脱敏支持包，再转交开发者定位和修复。

后台可以和上述 API 在同一个项目里，通过同一套用户、角色、审计和数据库实现。

第一版后台页面：

- 登录页
- 用户管理
- 组织/团队管理
- 订阅管理
- 套餐管理
- 订单/支付记录
- 设备管理
- License 管理
- 用量记录
- 封禁/解封
- 支持包查看
- 操作审计日志

### 8.5 后台角色

| 后台角色    | 权限                                                                                                   |
| ----------- | ------------------------------------------------------------------------------------------------------ |
| `admin`     | 全部管理权限。                                                                                         |
| `ops`       | 用户、订阅、订单、设备、用量、封禁和问题工单管理。                                                     |
| `developer` | 查看开发排障所需的脱敏支持包、诊断数据、错误、版本和灰度开关；不建议拥有套餐、订单、退款等运营写权限。 |

### 8.6 后台不应该做的事

后台不建议直接远程控制用户本地任务，例如直接启动、停止用户电脑上的自动化任务。第一版只管理授权和必要元数据。

如未来需要远程控制，也必须单独设计：

- 用户授权
- 远程命令队列
- 命令签名
- 操作审计
- 可撤销机制
- 明确的安全边界

### 8.7 什么时候再拆分

第一版不需要拆成多个服务。后续出现以下情况时，再考虑把 API、管理后台、支付或 License 服务拆开：

- API 请求量很大，管理后台和 PC 端授权请求互相影响。
- 支付和 License 需要独立部署、独立权限或独立审计。
- 多个产品共用同一套订阅系统。
- 管理后台权限复杂到需要单独前端团队维护。
- 企业客户或合规要求要求更强隔离。
- 服务端发布频率不同，例如管理后台频繁迭代，但 License 服务要求极高稳定性。

拆分前的推荐结构仍是：

```text
PC 桌面端
  ↓
account-matrix-server
  - 用户登录 API
  - License 校验 API
  - 设备绑定 API
  - 用量上报 API
  - 订阅/订单 API
  - 支付 webhook
  - Web 管理后台页面
  ↓
数据库
```

## 9. 套餐和功能开关

套餐建议使用功能开关驱动，而不是硬编码角色。

示例：

| 功能       | Starter        | Pro      | Team | Enterprise |
| ---------- | -------------- | -------- | ---- | ---------- |
| 启用账号数 | 10             | 50       | 300  | 自定义     |
| 设备数     | 1              | 2        | 5    | 自定义     |
| 手动任务   | 支持           | 支持     | 支持 | 支持       |
| 自动调度   | 不支持或低额度 | 支持     | 支持 | 支持       |
| 目标号互动 | 不支持         | 有限额度 | 支持 | 自定义     |
| CSV 导出   | 不支持         | 支持     | 支持 | 支持       |
| 支持包     | 支持           | 支持     | 支持 | 支持       |
| 团队席位   | 不支持         | 不支持   | 支持 | 支持       |

功能开关示例：

```json
{
  "features": {
    "manual_task": true,
    "scheduler": true,
    "target_engagement": true,
    "gmail_setup": false,
    "browser_batch_create": true,
    "csv_export": true,
    "support_bundle": true,
    "diagnostics": false
  }
}
```

## 10. 用量校验

订阅制必须记录用量，否则无法可靠限制套餐。

建议记录：

- 当前启用账号数
- 当前绑定设备数
- 每日启动任务次数
- 每日实际执行账号数
- 每日目标互动次数
- 每日注册账号次数
- 调度任务触发次数
- CSV 导出次数
- 失败次数和失败率

启动任务前校验：

```text
订阅状态有效
  ↓
设备已绑定
  ↓
套餐支持该功能
  ↓
账号数未超限
  ↓
今日额度未超限
  ↓
当前无冲突任务
  ↓
允许启动
```

到期或欠费时建议：

- 允许登录。
- 允许查看历史记录和报表。
- 禁止启动新任务。
- 禁止启动调度。
- 禁止新增或启用超过免费额度的账号。
- 显示续费入口和到期说明。

## 11. 用户问题反馈与开发者排障流程

第一版不设置独立排障角色。用户遇到问题时，问题由管理员/运营承接，管理员/运营收集必要信息后转交开发者；开发者修复问题、打包发布新版本，再由用户更新后继续使用。

流程：

1. 用户在 PC 端遇到问题。
2. 用户通过“帮助与支持”页面生成脱敏支持包，或复制错误摘要、版本号、设备状态。
3. 用户联系管理员/运营。
4. 管理员/运营在后台查看该用户的订阅、设备、License、应用版本、最近用量和支持包摘要。
5. 管理员/运营判断是否是订阅到期、设备超限、账号额度超限、操作问题或环境问题。
6. 如果是产品缺陷或无法处理的问题，管理员/运营把问题描述、复现步骤和脱敏支持包转交开发者。
7. 开发者使用开发者排障权限查看必要日志、诊断数据和支持包，定位并修复。
8. 开发者发布新版本或热修复后，管理员/运营通知用户更新或重试。

PC 端应保留“帮助与支持”页面，但它只是用户反馈入口。可包含：

- 当前应用版本
- 当前订阅状态
- 当前设备绑定状态
- 最近任务错误摘要
- 一键生成脱敏支持包
- 复制错误摘要
- 常见问题
- 联系管理员/运营方式

开发者排障模式可开放：

- 技术日志
- runtime 诊断
- 查看详细脱敏支持包
- 查看脱敏 stdout/stderr
- 浏览器环境诊断
- 清理非活跃 `run.lock`

仍应谨慎开放：

- 原始代理
- 明文路径
- 密码/凭据
- 清理账号数据
- 迁移配置
- 强制停止任务

后续如果出现专门客服、售后或外包排障人员，再考虑新增更细的排障权限角色。当前小团队阶段不需要。

## 12. 最小可行落地路线

### 阶段 1：PC 端权限壳

- 增加登录页或 License 激活页。
- 增加本地 License 缓存。
- 路由加入 `roles/features`。
- 普通/高级/开发者排障页面分组；系统设置中的高级路径、诊断、迁移、危险操作按角色和功能开关控制。
- 隐藏诊断、日志、路径、迁移和危险操作。

### 阶段 2：Tauri 命令校验

- 所有关键命令增加权限校验。
- 启动任务前检查套餐限制。
- 保存账号前检查启用账号数。
- 调度、目标号互动、导出等按功能开关控制。

### 阶段 3：管理/授权服务端

- 新增 `account-matrix-server`。
- 用户、组织、套餐、订阅、设备、License。
- 在线 License 签发和刷新。
- 用量上报。
- 到期和离线宽限策略。
- 后台登录、用户管理、订阅管理、设备管理。

### 阶段 4：后台和问题处理能力完善

- 管理用户、订阅、设备、License、订单、用量。
- 支持包查看。
- 封禁/解封。
- 审计日志。

### 阶段 5：支付闭环

- 接入支付渠道。
- 处理支付 webhook。
- 自动开通、续费、取消、退款、到期。
- 用户订阅中心展示真实订单和套餐状态。

## 13. 总结

一个 PC 应用可以解决用户侧的普通/高级能力，也可以保留开发者排障入口，但不能替代管理后台。

合理架构是：

```text
PC 桌面端
  - 给订阅用户使用
  - 根据 License 展示普通/高级能力
  - 为开发者保留受控排障入口
  - 本地执行任务
  - 上报用量

account-matrix-server
  - 统一登录
  - 订阅校验
  - License 签发
  - 设备绑定
  - 用量统计
  - 支付事件处理
  - Web 管理后台
  - 管理所有订阅用户及必要数据
  - 不直接承载用户本地自动化任务
```

第一版商业化可以先做简化：PC 端 License 激活 + 一个合并的 `account-matrix-server` 做订阅校验、后台用户/订阅/设备管理和基础用量。等付费链路跑通后，再扩展完整支付、团队权限、支持包和精细用量；只有规模或合规要求变高时，再考虑拆成多个服务。
